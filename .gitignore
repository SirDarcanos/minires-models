/bayesian_optimization
/.ipynb_checkpoints
__pycache__/
/temp

# Ignore local datasets and model downloads by default. Only the explicitly
# anonymized combined-dataset artifacts below are versioned; data subfolders
# and every other data file remain ignored.
/data/*
!/data/checksums.json
!/data/manifest.json
!/data/provenance.json
!/data/rejected.jsonl
!/data/test.jsonl
!/data/train.jsonl
!/data/validation.jsonl

.DS_Store
.venv/
.mypy_cache/
.pi/
private/
*.parquet
/scanned_minis-*.jsonl
/scanned_minis-*.metadata.json