# Artist privacy

Files and published outputs must not identify specific artists.
Artist identity is private and is not needed as a model input.

## Agent skills

### Issue tracker

Use GitHub Issues. Before ticket operations, read
`docs/agents/issue-tracker.md`.

### Triage labels

Use the repository's documented labels. Before triaging, read
`docs/agents/triage-labels.md`.

### Domain docs

Use a single-context layout. Before exploring the codebase, read
`docs/agents/domain.md`.
